<%= form_for [@vote, @story], :html => {:class => 'vote_for_stories'}, url: "/upvote/#{@story.id}" do |f| %>
<%= f.button "Upvote", value: true, class: "button round", id: "vote-up-button" %>
<% end %>
